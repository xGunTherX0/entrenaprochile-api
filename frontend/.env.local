# Local env file for development. Do NOT commit production secrets here.
# On Netlify this file should be ignored and the site should use the
# environment variables configured in Site settings → Build & deploy → Environment.

# Example (local dev only):
# VITE_API_BASE=http://localhost:5000
# VITE_GOOGLE_CLIENT_ID=346951825755-xxxxxxxxxxxxxxxxxxxxxxxx.apps.googleusercontent.com
